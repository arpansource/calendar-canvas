import { TimeGutterProps } from "../../../types";

const TimeGutter: React.FC<TimeGutterProps> = ({ className, style }) => {
  return <div className={className} style={style} />;
};
export default TimeGutter;
